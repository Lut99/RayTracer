CC=cl
ARGS=/std=c++17 /favor:INTEL64 /Wall /O2

SRC=src
BIN=bin/win
OBJ=$(BIN)/obj
LIB=$(SRC)/lib

OPTS=
EXT_LIBS=

LIBRARIES=

!IFDEF DEBUG
OPTS=/DEBUG
!ENDIF

!IFDEF THREADED
LIBRARIES += $(OBJ)/ThreadPoolWin.o
EXT_LIBS += -lpthread
OPTS=/D RENDER_THREADED
!ENDIF

